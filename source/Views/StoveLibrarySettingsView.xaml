<UserControl x:Class="StoveLibrary.Views.StoveLibrarySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="600">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Label Content="Profile URL:" VerticalAlignment="Center"/>
            <TextBox Width="420"
                     Text="{Binding Settings.ProfileUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="10,0,0,0"/>
        </StackPanel>

        <CheckBox Grid.Row="2"
                  Content="Import metadata"
                  IsChecked="{Binding Settings.ImportMetadata, Mode=TwoWay}"/>

        <CheckBox Grid.Row="4"
                  Content="Import tags"
                  IsChecked="{Binding Settings.ImportTags, Mode=TwoWay}"/>

        <CheckBox Grid.Row="6"
                  Content="Allow 18+ STOVE titles"
                  IsChecked="{Binding Settings.AllowAdultGames, Mode=TwoWay}"
                  ToolTip="If checked, the plugin will set the ADULT_GAME_AGREE cookie up-front so adult games can be scanned without the confirmation page."/>
    </Grid>
</UserControl>
